# Artisan Paint-by-Numbers Frontend Configuration

# Primary API URL (local ngrok tunnel when your machine is running)
NEXT_PUBLIC_API_URL=https://your-ngrok-url.ngrok-free.app

# Fallback API URL (cloud backend when local is unavailable)
# Options: Modal, Railway, Render, or any cloud hosting
NEXT_PUBLIC_FALLBACK_API_URL=https://your-modal-app.modal.run

# Stripe publishable key (for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Auth configuration
AUTH_SECRET=your-auth-secret-here
